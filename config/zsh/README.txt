-- Copy to Linux Lab PC
install.sh -> ~/install.sh

-- Install zsh-5.9 and ncurses-6.2, then export to PATH
-- Clone zsh and powerlevel10k to ~
git clone https://github.com/ohmyzsh/ohmyzsh.git
git clone https://github.com/romkatv/powerlevel10k

-- Run ~/install.sh to install ohmyzsh

-- Copy to Linux Lab PC (replace file to use my tokyonight theme ^^)
zshrc -> ~/.zshrc
p10k.zsh -> .p10k.zsh
